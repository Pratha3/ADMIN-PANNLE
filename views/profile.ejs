<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile Page</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #121212;
      /* Dark background for body */
    }
  </style>
</head>

<body class="text-gray-200">
  <div class="flex items-center justify-center min-h-screen">
    <div class="bg-gray-800 p-8 rounded-lg shadow-lg w-96">
      <h1 class="text-2xl font-bold mb-6 text-center text-white">Profile</h1>
      <form action="/profile" method="post">
        <ul class="space-y-6">
          <li class="flex items-center">
            <span class="font-semibold w-1/3 text-gray-400">Username:</span>
            <span class="w-2/3">
              <%= user.username %>
            </span>
          </li>
          <li class="flex items-center">
            <span class="font-semibold w-1/3 text-gray-400">Email:</span>
            <span class="w-2/3">
              <%= user.email %>
            </span>
          </li>
          <li class="flex items-center">
            <span class="font-semibold w-1/3 text-gray-400">Phone:</span>
            <span class="w-2/3">
              <%= user.phone %>
            </span>
          </li>
        </ul>
        <!-- Optional: Add a button for further actions -->
        <div class="flex justify-center mt-4">
          <a href="/edit-profile" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Edit Profile</a>
        </div>
      </form>
    </div>
  </div>
</body>

</html>