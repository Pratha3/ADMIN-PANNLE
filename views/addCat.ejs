<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Extra Category</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #121212;
            /* Dark background color */
        }

        .bg-gray-800 {
            background-color: #1f2937;
            /* Darker background color */
        }

        .text-gray-100 {
            color: #f9fafb;
            /* Light text color */
        }

        .text-gray-300 {
            color: #d1d5db;
            /* Slightly lighter text color */
        }

        .bg-gray-900 {
            background-color: #111827;
            /* Dark background for input */
        }

        .border-gray-600 {
            border-color: #4b5563;
            /* Dark border color */
        }

        .hover\:bg-blue-700:hover {
            background-color: #2563eb;
            /* Darker blue on hover */
        }

        .focus\:shadow-outline:focus {
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.5);
            /* Focus shadow */
        }
    </style>
</head>

<body class="h-screen flex items-start justify-center pt-12">
    <div class="bg-gray-800 shadow-md rounded-lg p-6 w-full max-w-sm">
        <h2 class="text-2xl font-bold mb-4 text-gray-100">Add Extra Category</h2>
        <form action="/extra/addextracategory" method="post">
            <div class="mb-4">
                <label for="extraCategoryName" class="block text-gray-300 font-semibold mb-2">Extra Category
                    Name</label>
                <input type="text" id="extraCategoryName" name="extracat_name"
                    class="shadow appearance-none border border-gray-600 rounded w-full py-2 px-3 text-gray-200 bg-gray-900 leading-tight focus:outline-none focus:shadow-outline"
                    placeholder="Enter extra category name" required>
            </div>
            <div class="flex items-center justify-between">
                <button type="submit"
                    class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">Add
                    Extra Category</button>
            </div>
        </form>
    </div>
</body>

</html>