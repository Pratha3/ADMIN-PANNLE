<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up Page</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <style>
        .custom-shape {
            background-color: #2d3748;
            /* Dark background color */
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            /* Subtle shadow for depth */
        }

        .input-field {
            background: #4a5568;
            /* Darker background for inputs */
            border-radius: 5px;
            padding-left: 1rem;
        }

        .container {
            max-width: 400px;
            /* Adjusted max-width */
            margin: 0 auto;
            padding: 2rem;
            /* Padding for spacing inside the container */
        }

        .hover-underline:hover {
            text-decoration: underline;
        }

        .hover-bg-orange:hover {
            background-color: #FF8C42;
        }

        .focus\:ring-pink-600:focus {
            box-shadow: 0 0 0 3px rgba(248, 113, 113, 0.5);
            /* Custom ring color */
        }

        .button {
            background-color: #e53e3e;
            /* Red button color */
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .button:hover {
            background-color: #c53030;
            /* Darker red on hover */
        }
    </style>
</head>

<body class="bg-gray-900 flex items-center justify-center min-h-screen">
    <div class="bg-gray-800 shadow-lg rounded-lg container custom-shape">
        <h2 class="text-4xl font-bold text-gray-100 mb-8 text-center">Sign Up</h2>
        <form action="/signup" method="post">
            <div class="mb-6 relative">
                <input type="text" id="username" name="username"
                    class="w-full px-4 py-2 border border-gray-600 rounded-lg text-gray-200 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-600 input-field"
                    placeholder="Enter your Username" required>
            </div>
            <div class="mb-6 relative">
                <input type="email" id="email" name="email"
                    class="w-full px-4 py-2 border border-gray-600 rounded-lg text-gray-200 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-600 input-field"
                    placeholder="Enter your Email" required>
            </div>
            <div class="mb-6 relative">
                <input type="password" id="password" name="password"
                    class="w-full px-4 py-2 border border-gray-600 rounded-lg text-gray-200 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-600 input-field"
                    placeholder="Enter your Password" required>
            </div>
            <div class="mb-6 relative">
                <input type="text" id="phone" name="phone"
                    class="w-full px-4 py-2 border border-gray-600 rounded-lg text-gray-200 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-pink-600 input-field"
                    placeholder="Enter your Phone" required>
            </div>
            <button type="submit" class="w-full text-white py-2 px-4 button">Sign Up</button>
        </form>
        <p class="mt-4 text-center text-gray-400">Already have an account? <a href="/login"
                class="text-orange-400 hover:text-orange-500 hover-underline">Login here</a></p>
    </div>
</body>

</html>